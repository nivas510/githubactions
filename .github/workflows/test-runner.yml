name: Test Self Hosted Runner

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: gh-runner

    steps:
      - name: Test runner
        run: |
          echo "Runner is working!"
          echo "Hostname:"
          hostname
          echo "User:"
          whoami
          echo "OS:"
          uname -a
